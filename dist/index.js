import d from "path";
import g from "dotenv";
import c from "fs";
import x from "prettier";
const y = (e) => e.global ? typeof e.global == "string" ? e.global : e.global() : "window", v = (e) => e.name ? typeof e.name == "string" ? e.name : e.name() : "env", E = (e) => {
  if (e.generatedTypesPath)
    return typeof e.generatedTypesPath == "string" ? e.generatedTypesPath : e.generatedTypesPath();
}, j = (e) => typeof e == "number" && !Number.isNaN(e) ? !0 : typeof e != "string" ? !1 : !Number.isNaN(Number(e)) && !Number.isNaN(Number.parseFloat(e)), T = (e) => typeof e == "boolean" ? !0 : typeof e != "string" ? !1 : !!["true", "1", "yes", "on", "false", "0", "no", "off"].includes(e.trim().toLowerCase()), $ = (e) => j(e) ? "number" : T(e) ? "boolean" : typeof e, h = (e, s) => ["MODE", "BASE_URL", "PROD", "DEV", "SSR"].includes(e) ? !0 : s.some((i) => e.startsWith(i)), _ = (e = { injectHtml: !0 }) => {
  let s = {}, o, i, u;
  const N = /(?:[$_\p{ID_Start}])(?:[$\u200C\u200D\p{ID_Continue}])*/u;
  return {
    name: "vite-plugin-runtime-env",
    configResolved(t) {
      o = t;
      let r = o.envPrefix ?? ["VITE_"];
      typeof r == "string" && (r = [r]), u = r, i = { ...e, ...o.runtimeEnv };
    },
    buildStart() {
      const t = d.resolve(o.root, o.envDir, ".env");
      c.existsSync(t) && (s = { ...s, ...g.parse(c.readFileSync(t)) });
      const r = d.resolve(o.root, o.envDir, `.env.${o.mode}`);
      if (c.existsSync(r) && (s = { ...s, ...g.parse(c.readFileSync(r)) }), Object.keys(s).forEach((m) => {
        h(m, u) && delete s[m];
      }), i.generateTypes && o.command === "serve") {
        const m = E(i) ?? o.root, n = v(i), l = d.resolve(m, `${n}.d.ts`), b = n === "env" ? "ImportMetaEnv" : "ImportMetaRuntimeEnv";
        let p = `/// <reference types="vite/client" />

/** generated by vite-plugin-runtime */
interface ${b} {`;
        Object.entries(s).forEach((f) => {
          p += `readonly ${f[0]}: ${$(f[1])};`;
        }), p += `} interface ImportMeta {readonly ${n}: ${b};}`, x.format(p, {
          semi: !0,
          singleQuote: !0,
          arrowParens: "avoid",
          tabWidth: 2,
          useTabs: !1,
          printWidth: 100,
          parser: "typescript"
        }).then((f) => c.writeFileSync(l, f));
      }
    },
    transform(t) {
      const r = y(i), a = v(i), m = new RegExp(`(import\\.meta\\.${a})(.+)`, "g");
      for (let n = m.exec(t); n !== null; n = m.exec(t)) {
        const l = N.exec(n[2]);
        if (l === null)
          continue;
        const b = l[0];
        if (h(b, u))
          continue;
        const p = n.index, f = p + n[1].length;
        t = t.slice(0, p) + `${r}.${a}` + t.slice(f);
      }
      return t;
    },
    transformIndexHtml() {
      if (i.injectHtml !== !0)
        return;
      const t = y(i), r = v(i);
      let a;
      return o.command === "serve" ? a = `${t}.${r} = {...${t}.${r}, ...${JSON.stringify(s)}};` : a = `import rtenv from '/${r}.js'; ${t}.${r} = {...${t}.${r}, ...rtenv};`, [
        {
          tag: "script",
          attrs: {
            type: "module"
          },
          children: a,
          injectTo: "head-prepend"
        }
      ];
    },
    generateBundle() {
      const t = v(i), r = {}, a = {};
      Object.entries(s).forEach((n) => {
        const l = $(n[1]);
        r[n[0]] = l === "number" ? Number(n[1]) : l === "boolean" ? !!n[1] : n[1], i.envsubstTemplate === !0 && (a[n[0]] = `$${n[0]}`);
      });
      const m = `export default ${JSON.stringify(r)} ;`;
      if (this.emitFile({
        type: "asset",
        fileName: `${t}.js`,
        source: m
      }), i.envsubstTemplate === !0) {
        const n = `export default ${JSON.stringify(a)} ;`;
        this.emitFile({
          type: "asset",
          fileName: `${t}.template.js`,
          source: n
        });
      }
    }
  };
}, D = (e) => {
  let s, o;
  return {
    name: "vite-plugin-runtime-html",
    configResolved(i) {
      s = i, o = { ...e, ...s.runtimeHtml };
    },
    transformIndexHtml() {
      return (o[s.mode] ?? []).map((u) => typeof u == "function" ? u() : u);
    }
  };
};
export {
  _ as runtimeEnv,
  D as runtimeHtml
};
